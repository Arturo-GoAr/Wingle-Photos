<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WinglePhotos.Features.PhotoSources.FoldersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:WinglePhotos.Features.PhotoSources"
    xmlns:shared="using:WinglePhotos.Shared"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <shared:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="12">
            <TextBlock Text="Carpetas" Style="{StaticResource TitleTextBlockStyle}" />
            <Button Content="Agregar carpeta…" Command="{x:Bind ViewModel.AddSourceCommand}" VerticalAlignment="Center" />
        </StackPanel>

        <ListView
            Grid.Row="1"
            ItemsSource="{x:Bind ViewModel.Sources}"
            SelectionMode="None"
            IsItemClickEnabled="True"
            ItemClick="FoldersListView_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:PhotoSource">
                    <Grid Padding="8" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <FontIcon Grid.Column="0" Glyph="&#xE8B7;" VerticalAlignment="Center" />
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="{x:Bind DisplayPath}" TextTrimming="CharacterEllipsis" />
                            <TextBlock
                                Text="No disponible"
                                Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Visibility="{x:Bind IsUnavailable, Converter={StaticResource BoolToVisibilityConverter}}" />
                        </StackPanel>
                        <Button
                            Grid.Column="2"
                            Background="Transparent"
                            BorderThickness="0"
                            VerticalAlignment="Center"
                            Visibility="{x:Bind CanRemove, Converter={StaticResource BoolToVisibilityConverter}}">
                            <FontIcon Glyph="&#xE712;" />
                            <Button.Flyout>
                                <MenuFlyout Placement="Bottom">
                                    <MenuFlyoutItem Text="Quitar carpeta" Tag="{x:Bind}" Click="RemoveFolder_Click">
                                        <MenuFlyoutItem.Icon>
                                            <FontIcon Glyph="&#xE74D;" />
                                        </MenuFlyoutItem.Icon>
                                    </MenuFlyoutItem>
                                </MenuFlyout>
                            </Button.Flyout>
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
